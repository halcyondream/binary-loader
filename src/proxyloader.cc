#include "proxyloader.h"
#include <cstdio>
#include <iostream>

void ProxyLoader::loadData(Data &data) {
  std::cout << "TODO: Implement dummy behavior for ProxyLoader::loadData()"
            << std::endl;
}